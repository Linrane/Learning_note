## 第4节

三参数公式：

```python
lst[起:止:步]
```

- **包含"起"，不包含"止"**（左闭右开，和 range 一致）
- 三个参数都可省略：省"起"从头、省"止"到尾、省"步"默认1

核心规则：左闭右开

![[Pasted image 20260719205708.png]]

![[Pasted image 20260719210423.png]]

注：切片是复制，而不是视图。（视图共有一份数据，内存里只有一个列表，此为引用共享，切片就是新开一个咯，new build，新建一个）

`b = a` 是让 b 指向同一个列表，改 b 会改 a；`b = a[:]` 才是复制一份。（重点）以下为具体的案例：

![[Pasted image 20260719211838.png]]

## 第5节

![[Pasted image 20260719213023.png]]

当你需要"用名字/编号去查对应值"时。通讯录、配置项、成绩单都是字典的天下。

创建字典的四种方式：

![[Pasted image 20260719213142.png]]

key不变，value唯一，后写覆盖先写，值类型不定。

![[Pasted image 20260719213748.png]]

不确定键是否存在时，**用 `.get()` 而不是 `[]`**，避免程序崩溃。（重点）

## 第6节

字典的增删改查：

| 操作   | 方式                      | 示例                      | 说明            |
| ---- | ----------------------- | ----------------------- | ------------- |
| 增/改  | `d[k]=v`                | `d["赵六"]=80`            | 键不存在则新增，存在则修改 |
| 查    | `d[k]` / `d.get(k)`     | `d["张三"]`               | get 更安全       |
| 删    | `del d[k]` / `d.pop(k)` | `del d["李四"]`           | pop 返回被删的值    |
| 批增   | `d.update(d2)`          | `d.update({"x":1})`     | 合并另一个字典       |
| 遍历键  | `d.keys()`              | `for k in d:`           | 最常用简写         |
| 遍历值  | `d.values()`            | `for v in d.values():`  |               |
| 遍历键值 | `d.items()`             | `for k,v in d.items():` | **最常用**       |

![[Pasted image 20260719222942.png]]

![[Pasted image 20260719222345.png]]

如果同时存在，后来的会覆盖掉前面的。

练习：

![[Pasted image 20260719223932.png]]

## 第7节

![[Pasted image 20260719224046.png]]

1. 数据一旦确定就不该变（坐标、RGB 颜色、日期）——不可变 = 安全
2. 需要作为字典的键（列表不行，元组可以）
3. 函数返回多个值 `return x, y`（本质是元组）

![[Pasted image 20260719224222.png]]

1. 只关心"某元素在不在"，不需要顺序
2. 需要去重
3. 需要交集/并集/差集

四大需求：

|需求|选谁|理由|
|---|---|---|
|按顺序存、要能改|`list`|有序可变|
|固定不变、要当字典键|`tuple`|不可变|
|用名字查值|`dict`|键值映射|
|去重 / 判断在不在|`set`|唯一 + O(1)查找|

练习：

![[Pasted image 20260719224934.png]]

## 第8节

查找速度：

|容器|查找方式|速度|复杂度|
|---|---|---|---|
|`list`|从头挨个比对|**慢**|O(n)：元素越多越慢|
|`dict`|按键哈希定位|**快**|O(1)：几乎不随元素增多变慢|
|`set`|哈希定位|**快**|O(1)|

- **list**：找 `x in lst` 要从第1个挨个比，最坏要比 n 次。1万个元素查1次，要比1万次。
- **dict/set**：基于**哈希表**，通过哈希函数直接算出 x 的位置，一步命中，几乎和元素数量无关。

练习：

![[Pasted image 20260719230126.png]]